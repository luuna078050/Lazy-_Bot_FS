services:
  - type: web
    name: fast-scalper-beta-001
    env: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.launcher
    autoDeploy: true
