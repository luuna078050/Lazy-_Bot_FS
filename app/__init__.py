"""LazyBot FS package bootstrap.

The Fast Scalper control surface lives in app.fixed_app. This module is kept
as a lightweight package marker so it does not replace the real Fast Scalper
UI with a second, reduced control surface.
"""
